```
#include <bits/stdc++.h>
using namespace std;
using pair<int, int> pii;

int n, m;
vector<pii> v;

void is_together(int a, int b)
{
    
}


void union_set(int a, int b)
{
    
}


int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    cin >> n >> m;
    for (int i=0; i<m; i++)
    {
        int order, a, b;
        cin >> order >> a >> b;
        if (order == 0)
        {
            
        }
        else
        {
            
        }
    }
  
    return 0;
}
```
